{
  "name": "pokemon-classic-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test:smoke": "playwright test --config=playwright.config.ts"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.4.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
